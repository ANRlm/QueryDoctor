[project]
name = "querydoctor-agent"
version = "0.1.0"
description = "QueryDoctor AI Agent"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
