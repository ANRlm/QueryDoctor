# QueryDoctor P0 开发计划

## TL;DR
> **Summary**: 搭建 QueryDoctor 核心骨架，实现数据库连接模块、LangGraph Agent 基础架构、慢查询分析核心逻辑、前端诊断界面。采用 Go Gin 网关 + Python FastAPI Agent + Redis Streams 通信架构。
> **Deliverables**: 完整项目骨架（前后端 + Agent）、Docker Compose 配置、可运行的 P0 功能演示
> **Effort**: Large
> **Parallel**: YES - 3 waves
> **Critical Path**: 项目骨架 → Agent 核心 → 前端界面 → 集成测试

## Context
### Original Request
从 Prompt.md 读取开发计划，P0 核心功能开始，Docker 部署为主。

### Interview Summary
- **起点**: P0 核心功能（数据库连接、LangGraph Agent、慢查询分析、前端界面）
- **部署**: Docker 部署为主
- **通信**: Go-Python 通过 Redis Streams（Oracle 建议），流式响应用 SSE
- **RAG**: P0 不包含

### Oracle Review (gaps addressed)
- **Redis 模式**: 选用 Streams + Consumer Groups（不是 Pub/Sub），确保消息持久化、可重放
- **LangGraph State**: 持久化到 PostgreSQL（JSONB），避免内存单点
- **容错机制**: 重试策略、死信队列（DLQ）、全链路健康检查
- **SSE 增强**: 心跳 ping、流量控制/背压、断线重连支持
- **观测性**: OpenTelemetry/Prometheus + 统一日志格式

## Work Objectives
### Core Objective
实现 QueryDoctor P0 核心功能：数据库连接、LangGraph Agent 架构、慢查询分析、基础前端界面

### Deliverables
- [ ] Go Gateway 项目骨架（cmd/gateway, internal/{config,server,proxy,db}）
- [ ] Python Agent 项目骨架（agent/{graph,nodes,api}）
- [ ] React 前端项目骨架（src/{features,components,services,store}）
- [ ] Docker Compose 配置（所有服务编排）
- [ ] MySQL/PostgreSQL 连接模块
- [ ] LangGraph StateGraph（collect→analyze→diagnose→suggest→END）
- [ ] SSE 流式诊断接口
- [ ] 基础诊断前端界面

### Definition of Done (verifiable conditions with commands)
- [ ] `curl http://localhost:8080/health` 返回 `{"status":"ok"}`
- [ ] `curl -X POST http://localhost:8080/api/diagnose -d '{"query":"SELECT * FROM users"}' -H "Content-Type: application/json"` 触发 SSE 流式响应
- [ ] 前端 `http://localhost:3000` 可访问诊断界面
- [ ] `docker compose ps` 所有服务 running
- [ ] `docker compose logs agent | grep "diagnos"` 显示诊断日志

### Must Have
- Go Gateway 处理 HTTP/WebSocket Upgrading，代理到 Python Agent
- Python FastAPI Agent 实现 LangGraph StateGraph
- Redis Streams 作为 Go-Python 通信中间件
- SSE 流式输出到前端
- 基础前端诊断界面（输入框 + 结果展示）

### Must NOT Have (guardrails, AI slop patterns, scope boundaries)
- NO RAG 知识库（P1 范围）
- NO 用户认证系统（P1 范围）
- NO Redis/MongoDB 连接器（P1 范围）
- NO 复杂前端图表（后续迭代）
- NO 生产级错误处理（基础异常捕获即可）

## Verification Strategy
> ZERO HUMAN INTERVENTION - all verification is agent-executed.
- Test decision: tests-after
- Framework: Go: unit test + integration test / Python: pytest
- QA policy: Every task has agent-executed scenarios
- Evidence: .sisyphus/evidence/task-{N}-{slug}.{ext}

## Execution Strategy
### Parallel Execution Waves
> Target: 5-8 tasks per wave. <3 per wave = under-splitting.

**Wave 1 (Foundation - 8 tasks)**: 项目骨架、Docker 配置、配置管理
**Wave 2 (Agent Core - 6 tasks)**: LangGraph 节点、FastAPI 接口、Redis 集成
**Wave 3 (Gateway + Frontend - 5 tasks)**: Go Gateway、SSE 代理、前端界面

### Dependency Matrix
| Task | Blocks | Blocked By |
|------|--------|------------|
| T1 项目目录结构 | T2, T3, T4 | - |
| T2 Docker 配置 | T5, T6 | T1 |
| T3 Go 项目骨架 | T5 | T1 |
| T4 Python 项目骨架 | T6 | T1 |
| T5 Go Gateway SSE 代理 | T8 | T2, T3 |
| T6 FastAPI Agent 接口 | T7, T8 | T2, T4 |
| T7 LangGraph StateGraph | T9 | T6 |
| T8 Redis Streams 集成 | T9 | T5, T6 |
| T9 数据库连接模块 | T10 | T3 |
| T10 前端诊断界面 | T11 | T5, T9 |
| T11 Docker Compose 编排 | T12 | T5, T6, T10 |
| T12 集成测试 | - | T11, T12 |

### Agent Dispatch Summary
- Wave 1: 3 个 build agent（并行创建目录结构）
- Wave 2: 2 个 build agent（Go + Python 各一）
- Wave 3: 2 个 build agent（Gateway + 前端各一）

## TODOs

- [ ] 1. 创建项目目录结构

  **What to do**: 创建完整项目目录树：
  ```
  QueryDoctor/
  ├── cmd/
  │   └── gateway/
  │       └── main.go
  ├── internal/
  │   ├── config/
  │   ├── server/
  │   │   ├── middleware/
  │   │   └── handler/
  │   ├── proxy/
  │   └── db/
  ├── agent/
  │   ├── agent/
  │   │   ├── graph/
  │   │   ├── nodes/
  │   │   └── state.py
  │   ├── api/
  │   │   └── routes.py
  │   └── main.py
  ├── frontend/
  │   └── src/
  │       ├── features/
  │       ├── components/
  │       ├── services/
  │       └── store/
  ├── docker/
  │   ├── gateway.Dockerfile
  │   ├── agent.Dockerfile
  │   ├── frontend.Dockerfile
  │   └── nginx.conf
  ├── docker-compose.yaml
  └── README.md
  ```

  **Must NOT do**: 不创建任何实现代码文件

  **Recommended Agent Profile**:
  - Category: `unspecified-low` - Reason: 纯目录创建，无技术复杂度
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 1 | Blocks: T2, T3, T4 | Blocked By: -

  **References**:
  - Pattern: N/A (新建项目)
  - Docker: 目录结构参考 https://github.com/golang-standards/project-layout
  - Frontend: 目录结构参考 https://github.com/vitejs/vite/blob/main/packages/create-vite/template-react-ts

  **Acceptance Criteria**:
  - [ ] 所有目录创建成功
  - [ ] README.md 包含项目说明
  - [ ] .gitkeep 文件存在于空目录

  **QA Scenarios**:
  ```
  Scenario: 目录结构验证
    Tool: Bash
    Steps: find QueryDoctor -type d | sort
    Expected: 包含所有必要目录
    Evidence: .sisyphus/evidence/task-1-dirs.txt
  ```

  **Commit**: YES | Message: `feat: initial project structure` | Files: [directories, README.md]

- [ ] 2. 创建 Docker 配置文件

  **What to do**: 创建所有 Docker 相关文件：
  - docker/gateway.Dockerfile (Go 1.21 Alpine)
  - docker/agent.Dockerfile (Python 3.10 Alpine)
  - docker/frontend.Dockerfile (Node 18 Alpine)
  - docker/nginx.conf (生产级配置，SSE 支持)
  - .dockerignore 文件

  **Must NOT do**: 不创建 docker-compose.yaml（单独任务）

  **Recommended Agent Profile**:
  - Category: `unspecified-high` - Reason: 需要多文件协调
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 1 | Blocks: T5, T6 | Blocked By: T1

  **References**:
  - Pattern: Go Dockerfile 参考 https://github.com/docker-library/golang
  - Python: Dockerfile 参考 https://github.com/docker-library/python
  - Nginx SSE: https://nginx.org/en/docs/http/ngx_http_proxy_module.html#proxy_cache_lock

  **Acceptance Criteria**:
  - [ ] gateway.Dockerfile 可构建成功
  - [ ] agent.Dockerfile 可构建成功
  - [ ] frontend.Dockerfile 可构建成功
  - [ ] nginx.conf 支持 SSE proxy

  **QA Scenarios**:
  ```
  Scenario: Docker 构建验证
    Tool: Bash
    Steps: docker build -f docker/gateway.Dockerfile -t querydoctor-gateway docker/
    Expected: 构建成功无错误
    Evidence: .sisyphus/evidence/task-2-docker-build.log
  ```

  **Commit**: YES | Message: `feat: add Docker configurations` | Files: [docker/*.Dockerfile, docker/nginx.conf, .dockerignore]

- [ ] 3. 创建 Go 项目骨架（cmd/gateway/main.go）

  **What to do**: 创建 Go Gateway 完整骨架：
  - cmd/gateway/main.go: 入口，Viper 配置加载，路由启动
  - internal/config/config.go: Viper 配置管理
  - internal/server/router.go: Gin 路由设置
  - internal/server/middleware/: auth.go, logging.go, error.go, recovery.go
  - internal/server/handler/: health.go
  - internal/proxy/http_proxy.go: ReverseProxy 到 FastAPI
  - internal/db/mysql.go, postgres.go: 连接池
  - internal/db/db_factory.go: 数据库工厂

  **Must NOT do**: 不实现业务逻辑，只做骨架

  **Recommended Agent Profile**:
  - Category: `unspecified-high` - Reason: Go 项目全套骨架
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 1 | Blocks: T5 | Blocked By: T1

  **References**:
  - Project layout: https://github.com/golang-standards/project-layout
  - Gin: https://github.com/gin-gonic/gin
  - Viper: https://github.com/spf13/viper
  - JWT: https://github.com/golang-jwt/jwt

  **Acceptance Criteria**:
  - [ ] `go build ./cmd/gateway` 成功
  - [ ] `/health` 端点返回 `{"status":"ok"}`
  - [ ] `/api/*path` 代理到 FastAPI (`http://agent:8000`)

  **QA Scenarios**:
  ```
  Scenario: Go 构建验证
    Tool: Bash
    Steps: cd backend && go build -o gateway ./cmd/gateway && ./gateway &
    Expected: 二进制运行成功
    Evidence: .sisyphus/evidence/task-3-go-build.log

  Scenario: Health 端点验证
    Tool: Bash
    Steps: curl http://localhost:8080/health
    Expected: {"status":"ok"}
    Evidence: .sisyphus/evidence/task-3-health.json
  ```

  **Commit**: YES | Message: `feat(gateway): add Go Gateway skeleton` | Files: [cmd/, internal/]

- [ ] 4. 创建 Python Agent 项目骨架

  **What to do**: 创建 Python Agent 完整骨架：
  - agent/main.py: FastAPI 入口，Uvicorn 启动
  - agent/config.py: 配置管理
  - agent/api/routes.py: API 路由定义
  - agent/graph/state.py: LangGraph State 定义
  - agent/graph/nodes/: 节点函数占位
  - agent/db/connector.py: 数据库连接器占位
  - requirements.txt: 依赖列表
  - pyproject.toml: 项目配置

  **Must NOT do**: 不实现 LangGraph 逻辑，只做骨架

  **Recommended Agent Profile**:
  - Category: `unspecified-high` - Reason: Python 项目全套骨架
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 1 | Blocks: T6 | Blocked By: T1

  **References**:
  - FastAPI: https://fastapi.tiangolo.com
  - LangGraph: https://github.com/langchain-ai/langgraph
  - Redis: https://github.com/redis/redis-py

  **Acceptance Criteria**:
  - [ ] `python -m agent` 启动 FastAPI
  - [ ] `/health` 端点返回 `{"status":"ok"}`
  - [ ] requirements.txt 包含 langgraph, fastapi, redis

  **QA Scenarios**:
  ```
  Scenario: Python 导入验证
    Tool: Bash
    Steps: cd agent && python -c "from api.routes import app; print('OK')"
    Expected: 输出 OK
    Evidence: .sisyphus/evidence/task-4-py-import.log
  ```

  **Commit**: YES | Message: `feat(agent): add Python Agent skeleton` | Files: [agent/]

- [ ] 5. 实现 Go Gateway SSE 代理和 Redis Streams 消费者

  **What to do**: 实现 Go Gateway SSE 功能：
  - internal/server/handler/sse.go: SSE 端点处理
  - internal/server/handler/diagnose.go: 诊断请求处理
  - internal/proxy/redis_streams.go: Redis Streams 消费者
  - internal/server/middleware/redis_streams.go: SSE 与 Redis 集成
  - SSE 心跳 ping 支持
  - 断线重连 event ID 支持

  **Must NOT do**: 不实现 FastAPI Agent 端点

  **Recommended Agent Profile**:
  - Category: `unspecified-high` - Reason: SSE + Redis Streams 集成
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 3 | Blocks: T11 | Blocked By: T2, T3

  **References**:
  - SSE spec: https://developer.mozilla.org/en-US/docs/Web/Server-Sent_Events
  - Redis Streams: https://redis.io/docs/data-types/streams/
  - Gin SSE: https://github.com/gin-gonic/gin/blob/master/binding/sse.go

  **Acceptance Criteria**:
  - [ ] `/api/diagnose` 返回 SSE 流
  - [ ] SSE 包含 `event: ping` 每 30 秒
  - [ ] Redis Streams 消费者正确接收消息

  **QA Scenarios**:
  ```
  Scenario: SSE 流验证
    Tool: Bash
    Steps: curl -N http://localhost:8080/api/diagnose -d '{"query":"SELECT 1"}' -H "Content-Type: application/json"
    Expected: 返回 SSE 格式流数据
    Evidence: .sisyphus/evidence/task-5-sse.log
  ```

  **Commit**: YES | Message: `feat(gateway): implement SSE proxy and Redis Streams consumer` | Files: [internal/server/handler/, internal/proxy/redis_streams.go]

- [ ] 6. 实现 FastAPI Agent SSE 流式接口

  **What to do**: 实现 Python Agent SSE 功能：
  - agent/api/routes.py: 添加 `/diagnose` SSE 端点
  - agent/api/sse_stream.py: SSE 流生成器
  - 与 Redis Streams 集成（XREADGROUP）

  **Must NOT do**: 不实现 LangGraph 节点

  **Recommended Agent Profile**:
  - Category: `unspecified-high` - Reason: FastAPI SSE 流式
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 3 | Blocks: T11 | Blocked By: T2, T4

  **References**:
  - FastAPI StreamingResponse: https://fastapi.tiangolo.com/advanced/stream-data/
  - LangGraph streaming: https://github.com/langchain-ai/langgraph/pull/6961

  **Acceptance Criteria**:
  - [ ] `/diagnose` 返回流式 JSON lines
  - [ ] 使用 `graph.stream(input, version="v2")`
  - [ ] Redis Streams 生产者正确发送消息

  **QA Scenarios**:
  ```
  Scenario: Agent SSE 验证
    Tool: Bash
    Steps: curl -N http://localhost:8000/diagnose -d '{"query":"SELECT 1"}' -H "Content-Type: application/json"
    Expected: 返回流式响应
    Evidence: .sisyphus/evidence/task-6-agent-sse.log
  ```

  **Commit**: YES | Message: `feat(agent): implement SSE streaming endpoint` | Files: [agent/api/routes.py, agent/api/sse_stream.py]

- [ ] 7. 实现 LangGraph StateGraph 核心架构

  **What to do**: 实现 LangGraph 核心：
  - agent/graph/state.py: TypedDict State 定义
  - agent/graph/nodes/collect.py: 收集查询节点
  - agent/graph/nodes/analyze.py: 分析节点（LLM 调用）
  - agent/graph/nodes/diagnose.py: 诊断节点
  - agent/graph/nodes/suggest.py: 建议节点
  - agent/graph/graph.py: StateGraph 组装和 compile
  - agent/graph/nodes/__init__.py

  **Must NOT do**: 不实现具体 LLM 调用逻辑（占位即可）

  **Recommended Agent Profile**:
  - Category: `unspecified-high` - Reason: LangGraph 核心实现
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 2 | Blocks: T8 | Blocked By: T6

  **References**:
  - StateGraph: https://github.com/langchain-ai/langgraph/blob/e931c686/libs/langgraph/langgraph/graph/state.py
  - Graph API: https://docs.langchain.com/langgraph
  - GraphRecursionError: https://github.com/langchain-ai/langgraph/blob/ed293f16d66ea6925dba95b4a763e050ac085322/libs/langgraph/langgraph/errors.py

  **Acceptance Criteria**:
  - [ ] StateGraph 包含 5 个节点（collect, analyze, diagnose, suggest, format）
  - [ ] `graph.compile()` 成功
  - [ ] `graph.stream(state)` 可执行

  **QA Scenarios**:
  ```
  Scenario: LangGraph 编译验证
    Tool: Bash
    Steps: cd agent && python -c "from graph.graph import compiled_graph; print('compiled:', compiled_graph is not None)"
    Expected: compiled: True
    Evidence: .sisyphus/evidence/task-7-langgraph.json
  ```

  **Commit**: YES | Message: `feat(agent): implement LangGraph StateGraph` | Files: [agent/graph/]

- [ ] 8. 实现 Redis Streams Go-Python 通信

  **What to do**: 实现 Redis Streams 集成：
  - Go 侧: 任务发布（XADD）、结果消费（XREADGROUP）
  - Python 侧: 任务消费（XREADGROUP）、结果发布（XADD）
  - Consumer Group: `diagnose-group`
  - Stream key: `diagnose:tasks`, `diagnose:results`
  - Ack 机制（XACK）

  **Must NOT do**: 不实现重试逻辑（P1）

  **Recommended Agent Profile**:
  - Category: `unspecified-high` - Reason: 跨语言 Redis Streams
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 2 | Blocks: T9 | Blocked By: T5, T6

  **References**:
  - Redis Streams: https://redis.io/docs/data-types/streams/
  - XREADGROUP: https://redis.io/commands/xreadgroup/

  **Acceptance Criteria**:
  - [ ] Go 发布任务到 `diagnose:tasks`
  - [ ] Python 消费任务并发布结果到 `diagnose:results`
  - [ ] Consumer Group `diagnose-group` 正确创建

  **QA Scenarios**:
  ```
  Scenario: Redis Streams 端到端验证
    Tool: Bash
    Steps: |
      redis-cli XGROUP CREATE diagnose:tasks diagnose-group 0
      redis-cli XADD diagnose:tasks * query "SELECT 1"
      redis-cli XREADGROUP GROUP diagnose-group consumer1 COUNT 1 STREAMS diagnose:tasks ">"
    Expected: 消息正确传递
    Evidence: .sisyphus/evidence/task-8-redis-streams.log
  ```

  **Commit**: YES | Message: `feat: implement Redis Streams communication` | Files: [Go internal/proxy/, Python agent/api/]

- [ ] 9. 实现数据库连接模块（MySQL/PostgreSQL）

  **What to do**: 实现数据库连接：
  - Go 侧: internal/db/mysql.go, postgres.go 连接池
  - Python 侧: agent/db/connector.py 连接器
  - 支持 EXPLAIN 解析
  - 连接池配置（MaxOpenConns, MaxIdleConns）

  **Must NOT do**: 不实现查询执行（只做连接测试）

  **Recommended Agent Profile**:
  - Category: `unspecified-high` - Reason: 数据库驱动集成
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 2 | Blocks: T10 | Blocked By: T3

  **References**:
  - Go MySQL: https://github.com/go-sql-driver/mysql
  - Go PG: https://github.com/lib/pq
  - Python MySQL: https://github.com/mysql/mysql-connector-python

  **Acceptance Criteria**:
  - [ ] MySQL 连接测试成功
  - [ ] PostgreSQL 连接测试成功
  - [ ] 连接池配置生效

  **QA Scenarios**:
  ```
  Scenario: 数据库连接验证
    Tool: Bash
    Steps: |
      docker compose exec gateway curl -X POST http://localhost:8080/api/db/test \
        -d '{"type":"mysql","host":"mysql","port":3306,"user":"root","password":"password","database":"test"}'
    Expected: {"status":"connected"}
    Evidence: .sisyphus/evidence/task-9-db-connect.json
  ```

  **Commit**: YES | Message: `feat: implement database connection modules` | Files: [internal/db/, agent/db/]

- [ ] 10. 实现前端诊断界面

  **What to do**: 实现 React 诊断界面：
  - src/features/diagnostics/DiagnosticsPage.tsx
  - src/components/forms/DiagnosticQueryForm.tsx
  - src/components/common/LoadingSkeleton.tsx
  - src/services/apiClient.ts: SSE 流读取
  - src/store/useDiagnosticsStore.ts: Zustand store
  - src/app/AppLayout.tsx: 布局组件
  - Vite 配置、路由配置

  **Must NOT do**: 不实现复杂图表

  **Recommended Agent Profile**:
  - Category: `visual-engineering` - Reason: React 组件开发
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 3 | Blocks: T11 | Blocked By: T5, T9

  **References**:
  - React + TypeScript + Vite: https://github.com/vitejs/vite/blob/main/packages/create-vite/template-react-ts
  - Ant Design: https://ant.design/components/overview
  - Zustand: https://github.com/pmndrs/zustand
  - SSE: https://developer.mozilla.org/en-US/docs/Web/API/EventSource

  **Acceptance Criteria**:
  - [ ] `http://localhost:3000/diagnostics` 可访问
  - [ ] 可输入 SQL 查询并提交
  - [ ] SSE 流式显示诊断结果

  **QA Scenarios**:
  ```
  Scenario: 前端界面验证
    Tool: Playwright
    Steps: |
      await page.goto('http://localhost:3000/diagnostics');
      await page.fill('textarea[name="query"]', 'SELECT * FROM users');
      await page.click('button[type="submit"]');
      await page.waitForSelector('.diagnostic-result', { timeout: 10000 });
    Expected: 界面正常显示并响应
    Evidence: .sisyphus/evidence/task-10-frontend.png
  ```

  **Commit**: YES | Message: `feat(frontend): implement diagnostics UI` | Files: [frontend/src/]

- [ ] 11. 创建 Docker Compose 编排配置

  **What to do**: 创建完整 docker-compose.yaml：
  - gateway 服务（Go）
  - agent 服务（Python FastAPI）
  - frontend 服务（React + Nginx）
  - redis 服务（Redis Streams）
  - mysql 服务（测试用）
  - postgres 服务（测试用）
  - nginx 服务（反向代理）
  - 网络配置、卷配置、环境变量

  **Must NOT do**: 不包含非 P0 服务

  **Recommended Agent Profile**:
  - Category: `unspecified-high` - Reason: Docker 编排配置
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: YES | Wave 3 | Blocks: T12 | Blocked By: T5, T6, T10

  **References**:
  - Docker Compose: https://docs.docker.com/compose/
  - Nginx SSE: https://nginx.org/en/docs/http/ngx_http_proxy_module.html

  **Acceptance Criteria**:
  - [ ] `docker compose config` 验证通过
  - [ ] 所有服务正确配置端口
  - [ ] 健康检查配置正确

  **QA Scenarios**:
  ```
  Scenario: Docker Compose 验证
    Tool: Bash
    Steps: docker compose config --quiet && echo "OK"
    Expected: 输出 OK
    Evidence: .sisyphus/evidence/task-11-compose.log
  ```

  **Commit**: YES | Message: `feat: add Docker Compose orchestration` | Files: [docker-compose.yaml]

- [ ] 12. 端到端集成测试

  **What to do**: 执行完整集成测试：
  - 健康检查测试
  - 诊断流程测试（curl → Gateway → Redis → Agent → SSE → 前端）
  - 日志验证
  - 资源清理

  **Must NOT do**: 不修复发现的问题（记录即可）

  **Recommended Agent Profile**:
  - Category: `unspecified-high` - Reason: 集成测试编排
  - Skills: [] 
  - Omitted: [] 

  **Parallelization**: Can Parallel: NO | Wave 3 | Blocks: - | Blocked By: T11

  **References**:
  - Integration test pattern: N/A

  **Acceptance Criteria**:
  - [ ] 所有服务启动成功
  - [ ] 诊断请求成功返回
  - [ ] 日志包含诊断记录

  **QA Scenarios**:
  ```
  Scenario: 完整诊断流程测试
    Tool: Bash
    Steps: |
      docker compose up -d
      sleep 10
      curl -X POST http://localhost:8080/api/diagnose \
        -H "Content-Type: application/json" \
        -d '{"query":"SELECT * FROM users WHERE id = 1"}' \
        -N 2>/dev/null | head -20
    Expected: 返回 SSE 流式诊断结果
    Evidence: .sisyphus/evidence/task-12-e2e.log
  ```

  **Commit**: NO | Message: N/A | Files: N/A

## Final Verification Wave (MANDATORY)
> 4 review agents run in PARALLEL. ALL must APPROVE.

- [ ] F1. Plan Compliance Audit — oracle
- [ ] F2. Code Quality Review — unspecified-high
- [ ] F3. Real Manual QA — unspecified-high (+ playwright if UI)
- [ ] F4. Scope Fidelity Check — deep

## Commit Strategy
- 每个 Wave 完成后提交一次
- 最终集成测试后提交最终版本
- Commit message 格式: `feat(scope): description`

## Success Criteria
1. 所有 12 个任务完成
2. Docker Compose 可启动所有 P0 服务
3. 诊断接口返回 SSE 流式响应
4. 前端界面可显示诊断结果
5. 基础日志输出正常